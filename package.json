{
  "name": "opspilot",
  "version": "0.1.0",
  "private": true,
  "description": "AI support and incident resolution agent",
  "scripts": {
    "build": "pnpm -r --workspace-concurrency=1 --if-present run build",
    "typecheck": "pnpm -r --workspace-concurrency=1 --if-present run typecheck && pnpm run agent:typecheck",
    "test": "pnpm -r --workspace-concurrency=1 --if-present run test && pnpm run agent:test",
    "clean": "pnpm -r --workspace-concurrency=1 --if-present run clean",
    "lint": "pnpm -r --if-present run lint",
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down",
    "infra:down:reset": "docker compose down -v",
    "db:test:ensure": "bash scripts/ensure-test-database.sh",
    "db:generate": "pnpm --filter @opspilot/database run db:generate",
    "db:migrate:dev": "pnpm --filter @opspilot/database run db:migrate:dev",
    "db:migrate:deploy": "pnpm --filter @opspilot/database run db:migrate:deploy",
    "db:migrate:status": "pnpm --filter @opspilot/database run db:migrate:status",
    "db:migrate:drift": "pnpm --filter @opspilot/database run db:migrate:drift",
    "db:migrate:test": "pnpm --filter @opspilot/database run db:migrate:test",
    "db:reset:dev": "pnpm --filter @opspilot/database run db:reset:dev",
    "db:reset:test": "pnpm --filter @opspilot/database run db:reset:test",
    "api:start": "pnpm --filter @opspilot/api run start",
    "api:start:dev": "pnpm --filter @opspilot/api run start:dev",
    "api:demo": "pnpm --filter @opspilot/api run demo",
    "test:integration:sequential": "pnpm --filter @opspilot/database run test:integration && pnpm --filter @opspilot/api run test:integration",
    "agent:preflight": "tsx scripts/agent/preflight.ts",
    "agent:verify": "tsx scripts/agent/verify.ts",
    "agent:scope-check": "tsx scripts/agent/scope-check.ts",
    "agent:review-bundle": "tsx scripts/agent/review-bundle.ts",
    "agent:codex-review": "tsx scripts/agent/codex-review.ts",
    "agent:typecheck": "tsc -p scripts/agent/tsconfig.json --noEmit",
    "agent:test": "vitest run scripts/agent"
  },
  "packageManager": "pnpm@11.13.1",
  "engines": {
    "node": ">=22.21.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}